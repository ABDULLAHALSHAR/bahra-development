# دليل النشر والربط — منصة بحرة إلكتريك لتطوير الموظفين

هذا الدليل يقسّم العمل إلى جزأين:

- **الجزء (أ):** رفع المنصة على GitHub Pages — يكفي لتشغيل المنصة كاملة أونلاين فوراً (بيانات محلية + مساعد يعمل بدون إنترنت).
- **الجزء (ب):** ربط Google Sheets + Apps Script + Groq — اختياري، لما تبي بيانات حيّة ومساعد ذكي عبر Groq.

---

## الجزء (أ): الرفع على GitHub Pages (١٠ دقائق)

1. أنشئ حساب على <https://github.com> إن ما كان عندك.
2. اضغط **New repository**، سمّه مثلاً `bahra-development`، اختر **Public**، ثم **Create repository**.
3. في الصفحة الجديدة اضغط **uploading an existing file**، ثم اسحب محتويات هذه الحزمة:
   - `index.html`
   - `.nojekyll`
   - مجلد `backend/` (اختياري — للمرجعية)
   - `README.md`
   ثم **Commit changes**.
4. افتح **Settings ▸ Pages**.
5. تحت **Build and deployment ▸ Source** اختر **Deploy from a branch**.
6. **Branch:** اختر `main` و `/ (root)` ثم **Save**.
7. انتظر دقيقة، ثم حدّث الصفحة — راح يظهر رابط موقعك:
   `https://<اسم-حسابك>.github.io/bahra-development/`
8. افتح الرابط — المنصة شغّالة. سجّل الدخول: مدير `1001` / مدرب `2001` / موظف `3001` — رمز PIN `1234`.

> ملاحظة: ملف `.nojekyll` مهم — يمنع GitHub من معالجة الملفات ويعرضها كما هي.

---

## الجزء (ب): تفعيل Groq + Google Sheets (اختياري)

### 1) مفتاح Groq
1. ادخل <https://console.groq.com> وسجّل.
2. **API Keys ▸ Create API Key**، انسخ المفتاح (يبدأ بـ `gsk_...`) واحفظه.

### 2) إنشاء قاعدة البيانات (Google Sheets)
1. أنشئ Google Sheet جديد باسم `Bahra HR DB`.
2. أنشئ ثلاثة تبويبات بهذه الأسماء **بالضبط**: `employees` و `courses` و `requests`.
3. ضع في الصف الأول من كل تبويب عناوين الأعمدة (مثال):
   - **employees:** `id | name | dept | position | role | perf | hours`
   - **courses:** `id | title | level | hours | lessons`
   - **requests:** `id | emp | course | date | status`
4. تقدر تعبّي بيانات موظفيك الحقيقيين هنا لاحقاً.

### 3) نشر الباك-إند (Apps Script)
1. من داخل الـ Sheet: **Extensions ▸ Apps Script**.
2. امسح المحتوى الافتراضي والصق كامل ملف `backend/Code.gs`.
3. **Project Settings (⚙️) ▸ Script Properties ▸ Add script property**:
   - Property: `GROQ_KEY`
   - Value: الصق مفتاح Groq
   - **Save**.
4. **Deploy ▸ New deployment ▸ (⚙️) Web app**:
   - Description: `bahra api`
   - Execute as: **Me**
   - Who has access: **Anyone**
   - **Deploy** ثم اقبل الصلاحيات.
5. انسخ **Web app URL** (ينتهي بـ `/exec`).

### 4) توصيل المنصة بالباك-إند
1. افتح `index.html` بمحرر نصوص (أو عدّله مباشرة على GitHub بالضغط على ✏️).
2. في أول أسطر السكربت، ابحث عن:
   ```js
   const CONFIG = { API_URL: "", GROQ_MODEL: "llama-3.3-70b-versatile" };
   ```
3. الصق رابط الـ Web App بين علامتي التنصيص:
   ```js
   const CONFIG = { API_URL: "https://script.google.com/macros/s/XXXX/exec", GROQ_MODEL: "llama-3.3-70b-versatile" };
   ```
4. احفظ (Commit) — الآن مساعد «اسألني» يجاوب عبر Groq. لو تركت `API_URL` فارغاً يرجع للوضع المحلي تلقائياً.

> **تنبيه أمني:** لا تضع مفتاح Groq داخل `index.html` أبداً — يبقى فقط في Script Properties داخل Apps Script.

---

## أسئلة شائعة

- **الموقع يفتح لكن بدون صور؟** تأكد أن ملف `index.html` مرفوع كما هو (الصور مدمجة داخله، ما تحتاج ملفات إضافية).
- **المساعد يرد ردود محلية فقط؟** يعني `API_URL` فارغ أو الرابط غير صحيح — أعد الجزء (ب).
- **أبغى بيانات موظفيّ الحقيقيين؟** عبّئ تبويب `employees` في الـ Sheet، وهذي نقطة الانطلاق لربط كل الشاشات بالبيانات الحيّة (نطوّرها في تحديث لاحق).
